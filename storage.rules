rules_version = '2';
// Default: per-user prefix. Adjust paths if uploads use a different layout.
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null
        && request.auth.uid == userId;
    }
  }
}
