# Environment Variables Template for FutureSeer
# Copy this to .env.local and fill in your actual API keys
# ALL VARIABLES ARE PRIVATE - DO NOT EXPOSE TO CLIENT

# Firebase Admin SDK Configuration (Server-side only)
FIREBASE_ADMIN_PROJECT_ID="your-project-id"
FIREBASE_ADMIN_CLIENT_EMAIL="your-service-account-email"
FIREBASE_ADMIN_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----"

# AstroApp API (Server-side only) - Hybrid Engine: AstroApp + Custom Fallback
ASTROAPP_EMAIL=your-astroapp-email
ASTROAPP_PASSWORD=your-astroapp-password
ASTROAPP_API_KEY=REDACTED_ASTROAPP_API_KEY

# Google Places API (Optional) - For enhanced place lookup and geocoding
GOOGLE_PLACES_API_KEY=your-google-places-api-key

# Timezone API (Optional) - For accurate timezone resolution
TIMEZONE_API_KEY=your-timezone-api-key

# Firebase Configuration (Client-side public keys)
NEXT_PUBLIC_FIREBASE_API_KEY=your-firebase-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id

# OpenAI API (if you have it)
OPENAI_API_KEY=your-openai-api-key

# Groq API (Ask the Seer + other fast AI; get key at https://console.groq.com)
GROQ_API_KEY=your-groq-api-key

# Seer tone dial (optional): subtle | elevated | oracle. Default: elevated
# SEER_TONE_MODE=elevated

# Vercel AI Gateway (optional - enables centralized monitoring and budget controls)
# Get your API key from: https://vercel.com/ai-gateway
AI_GATEWAY_API_KEY=your-ai-gateway-api-key

# Stability AI (for image generation)
STABILITY_API_KEY=your-stability-api-key

# Posthog 
POSTHOG_API_KEY=your-posthog-api-key

# Razorpay Payment Gateway (Server-side only)
RAZORPAY_KEY_ID=your-razorpay-key-id
RAZORPAY_KEY_SECRET=your-razorpay-key-secret

# Razorpay Webhook Secret (for verifying webhook signatures)
RAZORPAY_WEBHOOK_SECRET=your-razorpay-webhook-secret

# App Configuration
# NEXT_PUBLIC_APP_URL: canonical app URL for metadata, share links. Use https://futureseer.app in production.
NEXT_PUBLIC_APP_URL=http://localhost:3000
# NEXT_PUBLIC_BASE_URL: base URL for server-side API calls to this app. Match deployed URL (e.g. https://futureseer.app in production, http://localhost:3000 locally). On Vercel, optional if VERCEL_URL is set (auto-used as fallback).
NEXT_PUBLIC_BASE_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=FutureSeer

# Vercel production: set NEXT_PUBLIC_APP_URL and NEXT_PUBLIC_BASE_URL to your production domain (e.g. https://futureseer.app) in Project Settings > Environment Variables for Production (and Preview if desired).

# Ask the Seer rollout: all (default) | gated
# all = no gate, full rollout for all signed-in users
# gated = require askSeerBeta custom claim (set ASK_SEER_ROLLOUT=gated to re-enable gate if needed)
# ASK_SEER_ROLLOUT=all

# Optional when gated: comma-separated UIDs allowed without askSeerBeta claim (dev/local fallback)
# ASK_SEER_BETA_UIDS=uid1,uid2,uid3

# Founder (Server-side only) - Firebase Auth UID of FutureSeer founder; used for elevated community stats
FOUNDER_UID=your-firebase-auth-uid
# Optional: match founder by email if UID differs per environment
FOUNDER_EMAIL=your@email.com

# Razorpay Public Key (Client-side - safe to expose)
NEXT_PUBLIC_RAZORPAY_KEY_ID=your-razorpay-key-id

# Resend (notification emails + inbound support)
RESEND_API_KEY=your-resend-api-key
NOTIFICATION_FROM_EMAIL=notifications@yourdomain.com
# Webhook secret for Resend Inbound (emails to support@futureseer.app). Get it when creating the webhook in Resend dashboard.
RESEND_WEBHOOK_SECRET=whsec_xxxx
# Optional: forward copies of support emails to this address (e.g. your personal inbox)
SUPPORT_FORWARD_EMAIL=

# Cron secret (secure the cron route; set in Vercel env and send as Authorization: Bearer CRON_SECRET)
CRON_SECRET=your-cron-secret

# Affiliate - Amazon Associates tag (e.g. futureseer21-21 for Amazon.in)
NEXT_PUBLIC_AMAZON_AFFILIATE_TAG=futureseer21-21

# Affiliate - Astro-Charts birth chart calculator
NEXT_PUBLIC_ASTRO_CHARTS_URL=https://astro-charts.com

# Affiliate - region-specific Amazon domain (amazon.in for India, amazon.com, amazon.co.uk)
NEXT_PUBLIC_AMAZON_DOMAIN=amazon.in

# Certified gemstone seller - leave empty if no partner (GIA/IGI/GRS certified only, NOT Amazon)
NEXT_PUBLIC_CERTIFIED_GEMSTONE_SELLER_URL=

# Optional integrations (server-side only - never expose these as NEXT_PUBLIC_*)
# OpenWeatherMap - current weather for birth coordinates (https://openweathermap.org/api)
OPENWEATHER_API_KEY=
# NewsAPI.org - top headlines for mundane context / Seer (https://newsapi.org)
NEWS_API_KEY=
# GIPHY - GIF search in community (https://developers.giphy.com)
GIPHY_API_KEY=

# Growth layers (optional; omit or use 0/false — default off)
# Short URLs /l/angel-numbers → Learn (see lib/campaignShortLinks.ts)
# NEXT_PUBLIC_GROWTH_SHORT_LINKS_ENABLED=1
# Profile form draft in localStorage until mystical profile is generated
# NEXT_PUBLIC_GROWTH_PROFILE_DRAFT_ENABLED=1
# Server: stricter cap on POST /api/profile/generate-mystical per user per hour
# RATE_LIMIT_STRICT=1
# RATE_LIMIT_PROFILE_GEN_MAX_PER_HOUR=12
