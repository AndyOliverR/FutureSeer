/**
 * Report voice rule: all user-facing report text must be in second person.
 * FutureSeer speaks directly to the user as "you". The user's name must never appear in reports.
 */
export const REPORT_VOICE_RULE =
  'Write in second person only. Address the user as "you". Do not use the user\'s name anywhere in your response.';
