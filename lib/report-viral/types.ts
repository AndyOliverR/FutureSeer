/** Teaser payload for any tool (same shape as Western). */
export interface ToolTeaserPayload {
  archetypeName: string
  rarityLabel: string
  hookLine: string
  subLine: string
  patternName: string | null
}
